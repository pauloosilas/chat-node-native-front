import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  content: {
    paddingBottom: 100,
  },
  noChats: {
    color: "#666",
    textAlign: "center",
    marginTop: 20,
    opacity: 0.4,
    fontSize: 16,
    marginHorizontal: 40,
  },
});
